import XCTest
@testable import FirebaseIntegrator

final class FirebaseIntegratorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FirebaseIntegrator().text, "Hello, World!")
    }
}
